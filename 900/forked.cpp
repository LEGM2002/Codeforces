#include<bits/stdc++.h>
using namespace std;

int main(){
    int n, a, b, x_k, y_k, x_q, y_q, forks = 0;
    cin >> n;
    while(n--){
      forks = 0;
      cin >> a >> b;
      cin >> x_k >> y_k;
      cin >> x_q >> y_q;
  
      cout << forks << endl;
    }
    return 0;
}